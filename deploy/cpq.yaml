apiVersion: apps/v1
kind: Deployment
metadata:
  name: chronos-cpq

spec:
  replicas: 1

  selector:
    matchLabels:
      app: chronos-cpq

  template:
    metadata:
      labels:
        app: chronos-cpq

    spec:
      containers:
        - name: cpq

          image: 280480953008.dkr.ecr.us-west-2.amazonaws.com/chronos-dev:cpq-v2

          ports:
            - containerPort: 8080

          envFrom:
            - secretRef:
                name: chronos-cpq-db

          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5

          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10

---
apiVersion: v1
kind: Service
metadata:
  name: chronos-cpq

spec:
  selector:
    app: chronos-cpq

  ports:
    - port: 8080
      targetPort: 8080

  type: ClusterIP