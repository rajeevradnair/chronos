apiVersion: apps/v1
kind: Deployment
metadata:
  name: chronos-conversation
spec:
  replicas: 1

  selector:
    matchLabels:
      app: chronos-conversation

  template:
    metadata:
      labels:
        app: chronos-conversation

    spec:
      containers:
        - name: conversation

          image: 280480953008.dkr.ecr.us-west-2.amazonaws.com/chronos-dev:conversation-v1

          ports:
            - containerPort: 8080

          envFrom:
            - secretRef:
                name: chronos-conversation-db

          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5

          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10

---
apiVersion: v1
kind: Service
metadata:
  name: chronos-conversation

spec:
  selector:
    app: chronos-conversation

  ports:
    - port: 8080
      targetPort: 8080

  type: ClusterIP